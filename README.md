# DedupX

A fast all-in-one JavaScript runtime built with Bun.

## Install

```bash
bun install
```

## Run

```bash
bun run index.ts
```

Or with hot reload:

```bash
bun --hot src/index.ts
```

## Build

```bash
bun run build   # outputs 'dedupx' binary
bun run clean  # removes binary
```